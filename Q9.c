#include<stdio.h>

int main() { 
    int num ,den;
    printf("enter num");
    scanf("%d",&num);
    printf("enter the den");
    scanf(" %d", &den);
    printf(" the fraction is: %d/%d ",num, den);
    return 0;
}

