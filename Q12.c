#include<stdio.h>

int main() { 
    int r;
    float area;
    printf("enter the r",r);
    scanf("%d", &r);
    area = (22.0/7) *r*r;
    printf("the area is : %.2f",area);
    scanf(" %d %d",&r);

    return 0;
}

